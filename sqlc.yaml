version: "2"
sql:
  - schema: "sql/schema"
    queries: "sql/queries/users"
    engine: "postgresql"
    gen:
      go:
        out: "internal/repository/user"
        package: "user"
        emit_interface: true

